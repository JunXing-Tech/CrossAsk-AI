# CrossAsk 后端生产环境变量模板
# 用法：复制为 .env 并填入真实值，docker run 时用 --env-file .env 注入
# ⚠️ .env 含敏感信息，切勿提交到 git（已在 .gitignore 忽略）

# 通义千问 DashScope API Key
DASHSCOPE_API_KEY=your_dashscope_api_key

# MySQL（服务器已有实例，容器外）
MYSQL_USER=crossask_ai
CROSSASK_AI_MYSQL_PASSWORD=your_mysql_password
